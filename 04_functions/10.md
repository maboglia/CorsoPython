## 🔹 **Exercise: Functions**

### **Obiettivo**

Mettere in pratica la definizione di funzioni, l’uso di argomenti e valori di ritorno.

---

### **Compito**

1. Crea una funzione `calculate_total` che:

   * Prende una lista di prezzi come argomento.
   * Restituisce la somma totale dei prezzi.

2. Crea una funzione `apply_discount` che:

   * Prende un prezzo e una percentuale di sconto (default 10%).
   * Restituisce il prezzo scontato.

3. Crea una funzione `greet_customer` che:

   * Accetta il nome del cliente.
   * Stampa un messaggio di benvenuto: `"Welcome, <name>!"`.

---

### **Requisiti di output**

* Stampare il totale della lista di prezzi.
* Stampare il prezzo scontato di un articolo.
* Stampare il messaggio di benvenuto per un cliente.

